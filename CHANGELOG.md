# 변경 기록

## v1.8.15 · 2026-09-18

- 단계형 번호 표현 제거: `1. 사업자번호` → `사업자번호`
- `2. 파일명 정보` → `파일명 정보 수정`으로 변경
- 저장 버튼 아래 안내를 `파일명이 다르면 아래에서 수정할 수 있습니다.`로 변경
- 저장 버튼 위치, 파일명 생성, PDF 저장 로직은 변경하지 않음

## v1.8.14 · 2026-09-18

- `저장될 파일명` 미리보기를 `세금계산서 PDF 저장` 버튼 바로 위로 이동
- 파일명/저장/PDF 로직은 변경하지 않음

## v1.8.13 · 2026-09-18

- 모든 사이트 공통 PDF 규칙을 A4 세로(portrait)로 통일
- 계산서 본체 박스 우선 탐지 후 세로 A4에 맞춰 저장하며, 탐지 실패 시 기존 사이트별 fallback 유지
- 화면의 PDF 방향 선택 UI 제거
- `세금계산서 PDF 저장` 버튼을 사업자번호 카드 바로 아래로 이동해 스크롤 없이 빠르게 저장 가능
- 저장 버튼 활성화 조건, 파일명, 사이트별 추출/인증 로직은 그대로 유지

## v1.8.12 · 2026-09-18

- TaxBill365 계산서 crop PDF를 A4 세로(portrait)로 고정
- 화면 캡처가 가로로 넓어도 자동으로 A4 가로로 전환하지 않도록 수정
- TaxBill365 계산서 박스 crop, JPEG 변환, 파일명 로직은 그대로 유지
- 다른 사이트의 PDF 방향 선택 로직은 변경하지 않음

## v1.8.11 · 2026-09-18

- TaxBill365 crop PDF가 빈 문서로 보이던 문제 긴급 수정
- 원인: `Page.captureScreenshot`은 PNG로 캡처했지만 `screenshotPdf()`는 JPEG(`/DCTDecode`) 전용으로 PDF에 삽입하고 있었음
- TaxBill365 crop 캡처를 JPEG(quality 95)로 통일
- 계산서 영역 탐지/crop 로직과 기존 사이트 출력·파일명 로직은 그대로 유지

## v1.8.10 · 2026-09-18

- TaxBill365 출력 긴급 보강
- 원인: 계산서 본체가 여러 sibling table로 구성되어 가장 작은 공통 DOM 부모에도 발행정보·담당자 영역이 함께 포함되는 경우가 있었음
- TaxBill365에서는 DOM 부모를 숨기는 방식 대신 `전자계산서` 시작~`합계금액` 종료 사이의 실제 계산서 표 영역을 시각적으로 계산
- 계산서 표 영역만 screenshot crop 후 1페이지 PDF로 변환
- 발행상태·담당자·메일주소·국세청전송·인쇄/PDF/XML 영역은 crop 범위에서 제외
- 계산서 영역을 찾지 못하면 기존 printToPDF fallback 유지
- 기존 사이트별 출력 및 파일명 로직은 유지

## v1.8.9 · 2026-09-17

- 확장관리 화면용 T 아이콘을 1024px 원본에서 다시 생성하여 16/32/48/128/256/512px 제공
- PDF 저장 시 가능한 경우 세금계산서 본체의 가장 작은 공통 DOM 박스만 남겨 출력
- `공급자 / 공급받는자 / 품목 / 합계금액 / 작성일·공급가액·승인번호`를 모두 포함하는 박스를 계산서 본체로 판별
- 발행상태, 담당자, 국세청 전송상태, 메일주소, 인쇄/PDF/XML 버튼, 광고 등 부가정보가 포함된 큰 컨테이너는 후순위 처리
- 계산서 박스를 찾지 못하면 기존 사이트별/전체 화면 PDF fallback 유지
- 원본 페이지 DOM은 PDF 생성 중에만 임시 변경하고 finally에서 복원

## v1.8.8 · 2026-09-17

- eTradeBill (`*.etradebill.co.kr`) 전자세금계산서 전용 추출 adapter 추가
- 공급자 영역의 상호(업체명), 작성일자, 실제 품목행, 합계금액을 폼 value까지 포함해 읽도록 보강
- 공급받는자(학교)와 표 헤더를 업체명/품명으로 오인하는 문제 방지
- eTradeBill host permission 추가
- 기존 PDF 생성·다운로드·파일명 rename 로직은 변경하지 않음

## v1.8.7 · 2026-09-17

- 긴급 수정: 팝업 미리보기는 새 괄호형 파일명인데 실제 저장창은 이전 밑줄형 파일명으로 뜨는 문제 수정
- 원인: 사이트 PDF용 pending rename이 확장앱 자체 PDF 다운로드를 먼저 가로채던 우선순위 충돌
- 통합 PDF 저장 직전 과거 사이트 rename 상태를 제거
- 확장앱이 직접 생성한 PDF의 최신 파일명을 가장 먼저 적용하도록 다운로드 rename 우선순위 변경
- 파일명 규칙은 `세금계산서(YYMMDD_업체명_품명).pdf` 유지

## v1.8.6 · 2026-09-17

- 파일명 규칙을 `세금계산서(YYMMDD_업체명_품명).pdf`로 변경
- 작성일이 없으면 `세금계산서(업체명_품명).pdf`로 저장
- 팝업 미리보기와 실제 다운로드 파일명에 동일 규칙 적용
- PDF 생성·사이트별 출력·권한 로직은 v1.8.5 그대로 유지

## v1.8.5 · 2026-09-17

- 긴급 복구: 통합 `세금계산서 PDF 저장`에서 브라우저 기본 파일명 `다운로드.pdf`가 뜨는 문제 수정
- 확장앱이 직접 생성한 PDF 다운로드 직전에 계산된 세금계산서 파일명을 예약하고, 다운로드 1건에만 적용
- PDF 생성/사이트별 출력 로직은 v1.8.4 그대로 유지

## v1.8.4 · 2026-09-17

- PDF 저장 버튼을 `세금계산서 PDF 저장` 하나로 통합
- 사이트별 저장 방식 선택을 사용자 대신 내부 어댑터가 자동 처리
- 최신 단색 T 아이콘 적용
- v1.8.3의 eCount/FreeBill 권한 및 추출 로직 유지

## v1.8.3 · 2026-09-17

- eCount (`*.ecount.com`) 호스트 접근 권한 추가
- 일부 하위 프레임 권한 문제로 전체 정보 읽기가 중단될 때 최상위 프레임으로 재시도하는 안전장치 추가
- v1.8.2의 FreeBill / 사업자번호 3-2-5 / eCount 작성일 로직은 유지

## v1.8.2 · 2026-09-17

- FreeBill 계열의 하위 프레임에서도 계산서 정보를 읽을 수 있도록 `*.freebill.co.kr` 최소 호스트 권한을 추가했습니다.
- 사업자번호가 `3-2-5` 세 칸으로 나뉜 조회 화면에서도 저장된 번호를 자동 입력하도록 보강했습니다.
- eCount ERP 작성일이 `작성 / 년 / 월 / 일` 형태로 분리된 양식에서도 `YYYYMMDD`로 읽히도록 수정했습니다.
- FreeBill 분리형 작성일 fallback을 추가했습니다.
- 확장 아이콘을 투명 모서리의 단색 블루 `T` 아이콘으로 교체했습니다.

## v1.8.1 · 2026-09-17

- SmileEDI·eCount PDF 저장 직전 15초짜리 파일명 예약 추가
- 동일 사이트의 첫 PDF 1건 또는 확장프로그램이 직접 만든 PDF 1건에만 파일명 적용 후 즉시 예약 삭제
- XML·Excel·이미지 및 SmartBill·HomeTax·일반 사이트 다운로드는 사이트별 rename 대상에서 제외
- eCount 작성일 분리 셀 `YYYY / MM / DD` fallback 추가
- HomeTax 계산서 위 `인쇄`, `첨부보기` 버튼을 PDF 생성 중에만 숨기고 `finally`에서 복원
- manifest 권한 변경 없음

## v0.1.8 · 2026-09-16

- SmileEDI·eCount·SmartBill에만 적용되는 출력 adapter 추가
- SmileEDI에서 인증 후 DOM에 실제 인쇄 URL이 확인된 경우에만 인쇄 문서를 사용하고, 그 외에는 계산서 영역 캡처로 fallback
- SmileEDI의 비정상적으로 작은/빈 PDF 의심 결과를 다운로드하지 않도록 검증 추가
- eCount PDF 생성 중 `페이지설정` 창·overlay와 지정된 ERP 홍보 문구 영역만 일시 숨김, `finally`에서 style/class 복원
- SmartBill 일반 인쇄 문서가 확인되면 우선 사용하고, 아니면 계산서 표를 감싼 최소 영역만 출력
- `스마트인쇄` 외부 프로그램은 자동 실행 대상에서 제외
- 사이트 기능 클릭과 실제 PDF 다운로드 성공 상태를 분리
- 사업자번호 자동입력, 추출, 파일명, 수동 수정, `외` 판정, generic 출력 로직 유지

## v0.1.7 · 2026-09-16

- v0.1.5의 직접 정보 읽기·사이트 PDF·현재 화면 PDF 흐름 복구
- 사업자번호 자동입력을 전체 기능의 선행조건에서 분리
- SmileEDI 입력칸이 실제로 있을 때 하이픈 없는 번호를 최대 2회 입력 확인하고, 성공한 경우에만 `확인` 클릭
- 자동입력 실패 시 모든 입력칸과 `다시 읽기`·PDF 버튼을 계속 표시
- 계산서 내용이 있으면 인증 입력칸보다 계산서 분석을 우선
- 인증 후 자동 재분석은 고정된 2회만 시도하고 무한 대기 제거
- `외`는 품명 후보 수가 아니라 실제 품목 표의 유효한 행이 2개 이상일 때만 추가


## v0.1.5 · 2026-09-15

- 표에서 `년`, `월`, `일`과 날짜 숫자가 각각 다른 칸에 있는 작성일 형식 인식
- 작성일을 읽지 못해도 저장 버튼을 활성화하고 날짜 없이 파일명 생성
- 날짜 미인식 시 `날짜미확인` 문구를 파일명에 넣지 않도록 변경

## v0.1.4 · 2026-09-15

- SmileEDI의 `사업자번호를 입력` 안내 문구도 조회 화면으로 인식
- 사업자번호 입력·저장 영역을 파일명 정보보다 먼저 표시
- 조회 화면에서는 사업자번호 단계만 보여주고 세금계산서가 열린 뒤 파일명 정보를 읽도록 순서 수정
- 파일명의 작성일을 `YYMMDD` 형식으로 변경

## v0.1.3 · 2026-09-15

- 파일명 순서를 `세금계산서_작성일_업체명_품명.pdf`로 변경
- 품목 1개일 때 품명 뒤에 `외`를 붙이지 않도록 수정
- 서로 다른 품목이 2개 이상일 때만 첫 번째 품명 뒤에 `외` 표시
- 동일 품목이 여러 경로에서 중복 인식되어도 1개로 계산하도록 보강

## v0.1.2 · 2026-09-15

- `상호 (법인명)`에서 `(법인명)`을 업체명으로 잘못 읽던 문제 수정
- 품목 표의 다음 열 제목 `규격`을 품명으로 잘못 읽던 문제 수정
- `2026/09/14`를 `20260901`로 잘못 변환하던 날짜 정규식 수정
- `합계금액` 아래 행의 `660,000원`을 읽는 세로형 표 인식 추가
- 표의 열 제목과 실제 데이터 행을 구분하도록 인식 우선순위 보강

## v0.1.1 · 2026-09-15

- 보안메일 인증 화면을 세금계산서 이전 단계로 인식
- 저장된 사업자번호를 확장프로그램 실행 즉시 자동 입력
- 보안메일의 `확인` 버튼 자동 클릭
- URL 형식 검사 때문에 정상 페이지를 거부하던 문제 수정
- 사업자번호가 없을 때 최초 1회 설정 영역 자동 열기

## v0.1.0 · 2026-09-15

- 여러 사이트에서 사용할 수 있는 공통 세금계산서 정보 인식 기능
- 업체명·품명·작성일 수정 및 저장 파일명 미리보기
- `세금계산서_업체명_품명_작성일.pdf` 형식의 직접 PDF 저장
- 사이트의 원본 PDF 다운로드 버튼 탐색과 실제 다운로드 파일명 변경
- 원본 PDF가 없는 사이트를 위한 현재 화면 PDF 생성 보조 기능
- 같은 파일명 충돌 시 자동으로 번호를 붙여 덮어쓰기 방지
- 사업자번호 브라우저 내부 저장 및 현재 페이지 자동입력
- PDF 자동·가로·세로 방향 선택

# 첫 사용 확인표

세금계산서 사이트마다 화면 구조가 달라서, 처음 보는 사이트는 아래 네 가지만 확인하면 됩니다.

## 사이트별 확인

- [ ] SmileEDI 인증 전 화면에서 저장된 사업자번호가 하이픈 없이 입력되는가
- [ ] 자동입력이 실패해도 파일명 입력·다시 읽기·PDF 버튼이 그대로 보이는가
- [ ] 인증 후 계산서가 열리면 업체명·품명·작성일·합계금액을 읽는가
- [ ] 사업자번호 입력칸이 없는 일반 페이지에서 인증 경고 없이 바로 분석하는가
- [ ] 업체명이 공급받는 학교명이 아니라 실제 공급업체명으로 인식되는가
- [ ] 품명이 대표 품명으로 짧게 표시되는가
- [ ] 실제 품목 1행이면 `외`가 없고, 2행 이상일 때만 `첫 품명 외`로 표시되는가
- [ ] 작성일이 있으면 `YYYYMMDD` 8자리로 표시되는가
- [ ] 작성일을 못 읽어도 날짜 없이 PDF 저장이 가능한가
- [ ] PDF가 잘리지 않고 읽을 수 있게 저장되는가

## 문제가 있을 때 남길 정보

다음 버전에서 사이트 전용 규칙을 추가하려면 개인정보를 가린 뒤 아래 화면을 준비합니다.

1. 세금계산서 전체 화면
2. 확장프로그램이 읽어온 업체명·품명·작성일
3. 실제로 저장된 PDF 첫 페이지
4. 해당 세금계산서 사이트의 주소 중 도메인 부분

사업자번호, 이메일, 승인번호, 담당자 이름 등은 가려도 됩니다.

## 결과 표시 예시

- 정상: `업체명·품명·작성일 모두 정확 / PDF 1페이지 정상`
- 업체명 오류: `공급업체 대신 학교명이 들어옴`
- 품명 오류: `품명 대신 수량 또는 단가가 들어옴`
- PDF 오류: `오른쪽이 잘림 / 빈 페이지 / 2페이지로 나뉨`
# v0.1.8 출력 품질 확인

## SmileEDI

- [ ] 인증 전에도 다시 읽기·수동 입력·PDF 버튼을 사용할 수 있다.
- [ ] 인증 후 계산서가 보이면 인쇄 URL을 임의 생성하지 않는다.
- [ ] 검증된 사이트 인쇄 문서가 있으면 이를 우선 사용한다.
- [ ] 인쇄 문서가 없으면 계산서 영역 캡처 PDF가 저장된다.
- [ ] 1KB 안팎의 흰 PDF는 저장되지 않고 실패 안내가 표시된다.

## eCount

- [ ] 페이지설정 창이 열린 상태에서도 계산서 본문이 PDF에 나온다.
- [ ] 페이지설정 창과 backdrop은 PDF에 나오지 않는다.
- [ ] `좋은 ERP를 찾고 계신가요?` 배너가 PDF에 나오지 않는다.
- [ ] PDF 성공·실패 후 원본 페이지의 창·광고가 원래 상태로 복원된다.

## SmartBill

- [ ] 일반 `인쇄` 문서가 안전하게 확인되면 이를 사용한다.
- [ ] 외부 프로그램형 `스마트인쇄`는 자동 실행하지 않는다.
- [ ] 메뉴·사이드바·광고·담당자 정보·History·footer가 PDF에 나오지 않는다.
- [ ] 실제 계산서가 중심이 되어 1페이지로 저장된다.

## 공통 회귀

- [ ] 사업자번호 자동입력 실패가 UI를 막지 않는다.
- [ ] 다시 읽기·수동 수정·파일명 생성이 정상이다.
- [ ] 일반 사이트의 generic PDF가 정상이다.
- [ ] 실제 다운로드 시작 전 성공 메시지가 표시되지 않는다.
# v1.8.1 마무리 확인

## 파일명

- [ ] SmileEDI 저장 미리보기와 실제 PDF 파일명이 같다.
- [ ] eCount 저장 미리보기와 실제 PDF 파일명이 같다.
- [ ] 같은 파일명이 있으면 `(1)`, `(2)`로 저장된다.
- [ ] 15초 안에 다운로드가 없으면 이후 PDF 파일명에 영향을 주지 않는다.
- [ ] XML·Excel·이미지 다운로드 파일명은 바뀌지 않는다.
- [ ] SmartBill·HomeTax·일반 사이트 다운로드는 이번 예약 rename의 영향을 받지 않는다.

## eCount 작성일

- [ ] `2026 / 08 / 25` 분리 셀이 `20260825`로 표시된다.
- [ ] 파일명에는 `260825`가 들어간다.
- [ ] 업체명·품명·합계금액 인식은 기존과 같다.
- [ ] 작성일을 못 읽어도 날짜 없이 저장할 수 있다.

## HomeTax

- [ ] PDF에 `인쇄`, `첨부보기` 버튼이 나오지 않는다.
- [ ] PDF 생성 후 원래 화면에 두 버튼이 다시 표시된다.
- [ ] 두 버튼 이외의 계산서 내용은 숨겨지지 않는다.

# v1.8.2 확인

- [ ] FreeBill에서 업체명·품명·작성일·합계금액 읽힘
- [ ] FreeBill 하위 프레임 페이지에서 다시 읽기 정상 작동
- [ ] 사업자번호 3칸 입력 화면에서 `120 / 83 / 00063`으로 자동 입력
- [ ] 해당 사업자번호 영역의 확인 버튼만 눌림
- [ ] eCount `작성 / 년 / 월 / 일` 양식에서 작성일 `YYYYMMDD` 인식
- [ ] SmileEDI/eCount PDF 저장 및 파일명 로직 회귀 없음
- [ ] 아이콘 모서리가 투명하게 표시됨
